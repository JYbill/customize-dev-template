jobs:
  docker:
    name: build push
    runs-on: ubuntu-latest

    steps:
      # setup-node插件
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          # 支持: npm, yarn, pnpm，将缓存node_modules以及对应的包管理工具的缓存目录
          cache: "npm"
      - run: npm ci
